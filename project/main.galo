struct P_empty
    q float
    z bool
end

struct Point
    x int
    y int
    po P_empty
    gg byte
end

let p Point
p x = 99
p y = 100
p po q = 101
p po z = true
p gg = 11119

let x int = 20
let y int = x
x = (x + 12)

let a bool = true
let b bool = false
let c bool = (a or false)
let d bool = ((a or b) and c)
let e bool = (not a)

let f byte = 9
let g float = -99.1
#let h int = cast(int, f + g)

if ((not a) or b)
    let _A byte
elif (not a)
    let _A byte
    let _B byte
elif ((not c) or (x > 100))
    let _A byte
    let _B byte
    let _C byte
else
    let _A byte
    let _B byte
    if false
        let __A int
    end
    let _C byte
    let _D byte
end

let index int = 1

while (index < 10000) 
    if (index > 100)
        break
    end
    index = (index * 2)
    let _inner float = (index / 3)
end

#print(x)


##!galo interpret project/main.galo -- 1 5 9 3 -5 4 -99 123 
##!include project/lib.galo
#
#fun main(args list) int
#    let x int = 0
#    let i int = 0
#    while (list length(args) > i)
#        let arg string = list get(args, i)
#        let val int = int convert(arg)
#        if (val == -6)
#            print("INVALID ARGUMENT:", arg, "\n")
#            return 1
#        end
#        x = add(x, val)
#        i = increment(i)
#    end
#    print("args total up to: ", x, "\n")
#end